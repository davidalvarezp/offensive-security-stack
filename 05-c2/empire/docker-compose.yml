version: '3'
services:
  empire:
    image: bcsecurity/empire:latest
    volumes:
      - ./listeners:/empire/listeners
      - ./logs:/empire/logs
    ports:
      - "1337:1337"
    environment:
      - EMPIRE_HTTP_PORT=80
      - EMPIRE_HTTPS_PORT=443
      - EMPIRE_STAGER_PORT=8080
