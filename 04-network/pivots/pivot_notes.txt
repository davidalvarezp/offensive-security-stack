# Pivoting Notes (Lab Use Only)

This folder stores information related to pivoting inside lab networks:
- Target hosts
- Open ports
- Exploit results (lab only)
- SSH tunnels or proxychains setups

Examples of lab pivot commands (do NOT use on production):
# ssh -L 1080:targethost:80 user@bastion
# proxychains curl http://internal.lab.local
